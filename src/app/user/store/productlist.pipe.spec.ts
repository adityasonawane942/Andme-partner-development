import { ProductlistPipe } from './productlist.pipe';

describe('ProductlistPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductlistPipe();
    expect(pipe).toBeTruthy();
  });
});
